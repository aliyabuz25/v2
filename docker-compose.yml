version: '3.8'

services:
  onfuture:
    build: .
    container_name: onfuture-web
    restart: unless-stopped
    ports:
      - "6985:6985"
    environment:
      - NODE_ENV=production
    networks:
      - onfuture-network

networks:
  onfuture-network:
    driver: bridge
